class Splits:
    TRAIN = "train"  # used for training the model
    VALIDATION = "validation"  # used for validation during and after training
    TEST = "test"  # used for testing after training (unseen)
    SINGLE = "single"  # used as a placeholder when only one split is needed
